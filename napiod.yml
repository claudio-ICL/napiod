name: napiod
channels:
  - conda-forge
dependencies:
  - python
  - cython
  - numpy<1.22
  - scipy
  - ipykernel
  - matplotlib
  - pip 
  - pip:
    - build
# Try and keep this aligned and consistent with mpoints.yml

